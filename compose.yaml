name: truthguard

x-app-environment: &app-environment
  APP_NAME: ${APP_NAME:-TruthGuard}
  APP_ENV: ${APP_ENV:-production}
  APP_KEY: ${APP_KEY:?Set a valid Laravel APP_KEY in Dokploy}
  APP_DEBUG: ${APP_DEBUG:-false}
  APP_URL: ${APP_URL:?Set APP_URL in Dokploy}
  LOG_CHANNEL: ${LOG_CHANNEL:-stderr}
  LOG_LEVEL: ${LOG_LEVEL:-warning}
  PLAYWRIGHT_LOG_LEVEL: ${PLAYWRIGHT_LOG_LEVEL:-warning}
  TRUTHGUARD_ADMIN_EMAILS: ${TRUTHGUARD_ADMIN_EMAILS:-}
  TRUTHGUARD_PRIVACY_POLICY_VERSION: ${TRUTHGUARD_PRIVACY_POLICY_VERSION:-2026-07-28}
  TRUTHGUARD_ONBOARDING_VERSION: ${TRUTHGUARD_ONBOARDING_VERSION:-2026-07-28}
  TRUTHGUARD_MEDIA_UPLOAD_MAX_MB: ${TRUTHGUARD_MEDIA_UPLOAD_MAX_MB:-20}
  TRUTHGUARD_AVATAR_UPLOAD_MAX_MB: ${TRUTHGUARD_AVATAR_UPLOAD_MAX_MB:-5}
  TRUTHGUARD_CLAIM_MAX_CHARACTERS: ${TRUTHGUARD_CLAIM_MAX_CHARACTERS:-4000}
  DB_CONNECTION: ${DB_CONNECTION:-mysql}
  DB_HOST: ${DB_HOST:-db}
  DB_PORT: ${DB_PORT:-3306}
  DB_DATABASE: ${DB_DATABASE:-truthguard}
  DB_USERNAME: ${DB_USERNAME:-truthguard}
  DB_PASSWORD: ${DB_PASSWORD:?Set DB_PASSWORD in Dokploy}
  SESSION_DRIVER: ${SESSION_DRIVER:-database}
  SESSION_LIFETIME: ${SESSION_LIFETIME:-120}
  SESSION_EXPIRE_ON_CLOSE: ${SESSION_EXPIRE_ON_CLOSE:-true}
  SESSION_ENCRYPT: ${SESSION_ENCRYPT:-true}
  SESSION_SECURE_COOKIE: ${SESSION_SECURE_COOKIE:-true}
  SESSION_SAME_SITE: ${SESSION_SAME_SITE:-lax}
  CACHE_STORE: ${CACHE_STORE:-database}
  QUEUE_CONNECTION: ${QUEUE_CONNECTION:-database}
  DB_QUEUE_RETRY_AFTER: ${DB_QUEUE_RETRY_AFTER:-360}
  FILESYSTEM_DISK: ${FILESYSTEM_DISK:-local}
  MAIL_MAILER: ${MAIL_MAILER:-smtp}
  MAIL_SCHEME: ${MAIL_SCHEME:-smtps}
  MAIL_HOST: ${MAIL_HOST:-}
  MAIL_PORT: ${MAIL_PORT:-465}
  MAIL_USERNAME: ${MAIL_USERNAME:-}
  MAIL_PASSWORD: ${MAIL_PASSWORD:-}
  MAIL_FROM_ADDRESS: ${MAIL_FROM_ADDRESS:-}
  MAIL_FROM_NAME: ${MAIL_FROM_NAME:-TruthGuard}
  GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
  GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}
  GOOGLE_REDIRECT_URI: ${GOOGLE_REDIRECT_URI:-}
  GOOGLE_PICKER_CLIENT_ID: ${GOOGLE_PICKER_CLIENT_ID:-}
  GOOGLE_PICKER_API_KEY: ${GOOGLE_PICKER_API_KEY:-}
  GOOGLE_PICKER_APP_ID: ${GOOGLE_PICKER_APP_ID:-}
  GOOGLE_PICKER_SCOPE: ${GOOGLE_PICKER_SCOPE:-https://www.googleapis.com/auth/drive.readonly}
  FACEBOOK_CLIENT_ID: ${FACEBOOK_CLIENT_ID:-}
  FACEBOOK_CLIENT_SECRET: ${FACEBOOK_CLIENT_SECRET:-}
  FACEBOOK_REDIRECT_URI: ${FACEBOOK_REDIRECT_URI:-}
  FACEBOOK_PAGE_ID: ${FACEBOOK_PAGE_ID:-}
  FACEBOOK_PAGE_ACCESS_TOKEN: ${FACEBOOK_PAGE_ACCESS_TOKEN:-}
  FACEBOOK_GRAPH_VERSION: ${FACEBOOK_GRAPH_VERSION:-v26.0}
  FACEBOOK_WEBHOOK_VERIFY_TOKEN: ${FACEBOOK_WEBHOOK_VERIFY_TOKEN:-}
  FACEBOOK_AUTO_REPLY_ENABLED: ${FACEBOOK_AUTO_REPLY_ENABLED:-false}
  FACEBOOK_SYSTEM_USER_ID: ${FACEBOOK_SYSTEM_USER_ID:-}
  LIVE_VERIFICATION_ENABLED: ${LIVE_VERIFICATION_ENABLED:-true}
  OPENAI_API_KEY: ${OPENAI_API_KEY:-}
  OPENAI_MODEL: ${OPENAI_MODEL:-gpt-5.4}
  OPENAI_ANALYSIS_ENABLED: ${OPENAI_ANALYSIS_ENABLED:-true}
  OPENAI_CLAIM_EXTRACTION_ENABLED: ${OPENAI_CLAIM_EXTRACTION_ENABLED:-true}
  OPENAI_TIMEOUT: ${OPENAI_TIMEOUT:-40}
  OPENAI_MAX_COMPLETION_TOKENS: ${OPENAI_MAX_COMPLETION_TOKENS:-1400}
  OPENAI_IMAGE_CONTEXT_ENABLED: ${OPENAI_IMAGE_CONTEXT_ENABLED:-true}
  OPENAI_IMAGE_MAX_BYTES: ${OPENAI_IMAGE_MAX_BYTES:-4194304}
  OPENAI_SOCIAL_SEARCH_ENABLED: ${OPENAI_SOCIAL_SEARCH_ENABLED:-true}
  OPENAI_SOCIAL_SEARCH_CONTEXT_SIZE: ${OPENAI_SOCIAL_SEARCH_CONTEXT_SIZE:-medium}
  OPENAI_SOCIAL_SEARCH_MAX_RESULTS: ${OPENAI_SOCIAL_SEARCH_MAX_RESULTS:-4}
  OPENAI_SOCIAL_SEARCH_DOMAINS: ${OPENAI_SOCIAL_SEARCH_DOMAINS:-facebook.com,instagram.com,tiktok.com,x.com,twitter.com,youtube.com,reddit.com,threads.net}
  OPENAI_MONTHLY_TOKEN_BUDGET: ${OPENAI_MONTHLY_TOKEN_BUDGET:-0}
  OPENAI_ESTIMATED_INPUT_TOKENS_PER_REQUEST: ${OPENAI_ESTIMATED_INPUT_TOKENS_PER_REQUEST:-1800}
  OPENAI_ESTIMATED_OUTPUT_TOKENS_PER_REQUEST: ${OPENAI_ESTIMATED_OUTPUT_TOKENS_PER_REQUEST:-1400}
  OPENAI_TOKEN_WARNING_THRESHOLD: ${OPENAI_TOKEN_WARNING_THRESHOLD:-80}
  GOOGLE_FACT_CHECK_API_KEY: ${GOOGLE_FACT_CHECK_API_KEY:-}
  GOOGLE_FACT_CHECK_LANGUAGE_CODE: ${GOOGLE_FACT_CHECK_LANGUAGE_CODE:-en-US}
  GOOGLE_FACT_CHECK_PAGE_SIZE: ${GOOGLE_FACT_CHECK_PAGE_SIZE:-3}
  GOOGLE_FACT_CHECK_TIMEOUT: ${GOOGLE_FACT_CHECK_TIMEOUT:-12}
  GOOGLE_FACT_CHECK_FEED_CACHE_SECONDS: ${GOOGLE_FACT_CHECK_FEED_CACHE_SECONDS:-10}
  GOOGLE_FACT_CHECK_FEED_REFRESH_SECONDS: ${GOOGLE_FACT_CHECK_FEED_REFRESH_SECONDS:-10}
  GOOGLE_FACT_CHECK_FEED_DIRECT_SOURCES_ENABLED: ${GOOGLE_FACT_CHECK_FEED_DIRECT_SOURCES_ENABLED:-true}
  GOOGLE_FACT_CHECK_FEED_NEWS_NOTIFICATIONS_ENABLED: ${GOOGLE_FACT_CHECK_FEED_NEWS_NOTIFICATIONS_ENABLED:-true}
  GOOGLE_FACT_CHECK_FEED_NEWS_NOTIFICATIONS_SEED_BASELINE: ${GOOGLE_FACT_CHECK_FEED_NEWS_NOTIFICATIONS_SEED_BASELINE:-false}
  GOOGLE_FACT_CHECK_FEED_NEWS_NOTIFICATIONS_NOTIFY_EXISTING: ${GOOGLE_FACT_CHECK_FEED_NEWS_NOTIFICATIONS_NOTIFY_EXISTING:-false}
  GOOGLE_FACT_CHECK_FEED_NEWS_NOTIFICATION_LIMIT: ${GOOGLE_FACT_CHECK_FEED_NEWS_NOTIFICATION_LIMIT:-5}
  GOOGLE_FACT_CHECK_FEED_IMAGE_TIMEOUT: ${GOOGLE_FACT_CHECK_FEED_IMAGE_TIMEOUT:-1}
  GOOGLE_FACT_CHECK_FEED_IMAGE_MISS_CACHE_MINUTES: ${GOOGLE_FACT_CHECK_FEED_IMAGE_MISS_CACHE_MINUTES:-10}
  GNEWS_API_KEY: ${GNEWS_API_KEY:-}
  GNEWS_LANG: ${GNEWS_LANG:-en}
  GNEWS_MAX: ${GNEWS_MAX:-3}
  GNEWS_TIMEOUT: ${GNEWS_TIMEOUT:-12}
  GNEWS_LOOKBACK_HOURS: ${GNEWS_LOOKBACK_HOURS:-168}
  NEWSAPI_API_KEY: ${NEWSAPI_API_KEY:-}
  NEWSAPI_LANGUAGE: ${NEWSAPI_LANGUAGE:-en}
  NEWSAPI_PAGE_SIZE: ${NEWSAPI_PAGE_SIZE:-3}
  NEWSAPI_TIMEOUT: ${NEWSAPI_TIMEOUT:-12}
  NEWSAPI_LOOKBACK_DAYS: ${NEWSAPI_LOOKBACK_DAYS:-7}
  OPENWEATHER_API_KEY: ${OPENWEATHER_API_KEY:-}
  OPENWEATHER_UNITS: ${OPENWEATHER_UNITS:-metric}
  OPENWEATHER_LANG: ${OPENWEATHER_LANG:-en}
  OPENWEATHER_TIMEOUT: ${OPENWEATHER_TIMEOUT:-12}
  PLAYWRIGHT_ENABLED: ${PLAYWRIGHT_ENABLED:-true}
  PLAYWRIGHT_NODE_BINARY: ${PLAYWRIGHT_NODE_BINARY:-node}
  PLAYWRIGHT_SCRIPT_PATH: ${PLAYWRIGHT_SCRIPT_PATH:-automation/playwright/cli.mjs}
  PLAYWRIGHT_BROWSER: ${PLAYWRIGHT_BROWSER:-chromium}
  PLAYWRIGHT_HEADLESS: ${PLAYWRIGHT_HEADLESS:-true}
  PLAYWRIGHT_TIMEOUT_MS: ${PLAYWRIGHT_TIMEOUT_MS:-45000}
  PLAYWRIGHT_NAVIGATION_TIMEOUT_MS: ${PLAYWRIGHT_NAVIGATION_TIMEOUT_MS:-45000}
  PLAYWRIGHT_INITIAL_WAIT_MS: ${PLAYWRIGHT_INITIAL_WAIT_MS:-1500}
  PLAYWRIGHT_SCROLL_LIMIT: ${PLAYWRIGHT_SCROLL_LIMIT:-5}
  PLAYWRIGHT_SCROLL_PAUSE_MS: ${PLAYWRIGHT_SCROLL_PAUSE_MS:-1200}
  PLAYWRIGHT_POST_LIMIT: ${PLAYWRIGHT_POST_LIMIT:-10}
  PLAYWRIGHT_RETRIES: ${PLAYWRIGHT_RETRIES:-2}
  PLAYWRIGHT_TAKE_SCREENSHOT: ${PLAYWRIGHT_TAKE_SCREENSHOT:-false}
  PLAYWRIGHT_SCREENSHOT_DIR: ${PLAYWRIGHT_SCREENSHOT_DIR:-storage/app/private/playwright/screenshots}
  PLAYWRIGHT_DEFAULT_SOURCE: ${PLAYWRIGHT_DEFAULT_SOURCE:-generic-feed}
  PLAYWRIGHT_RELATED_POSTS_ENABLED: ${PLAYWRIGHT_RELATED_POSTS_ENABLED:-true}
  PLAYWRIGHT_RELATED_POSTS_SOURCE: ${PLAYWRIGHT_RELATED_POSTS_SOURCE:-generic-feed}
  PLAYWRIGHT_RELATED_POSTS_LIMIT: ${PLAYWRIGHT_RELATED_POSTS_LIMIT:-6}
  PLAYWRIGHT_RELATED_POSTS_TTL_HOURS: ${PLAYWRIGHT_RELATED_POSTS_TTL_HOURS:-6}
  PLAYWRIGHT_SOCIAL_CONTEXT_ENABLED: ${PLAYWRIGHT_SOCIAL_CONTEXT_ENABLED:-true}
  PLAYWRIGHT_SOCIAL_CONTEXT_POST_LIMIT: ${PLAYWRIGHT_SOCIAL_CONTEXT_POST_LIMIT:-2}
  PLAYWRIGHT_SOCIAL_CONTEXT_MAX_SOURCES: ${PLAYWRIGHT_SOCIAL_CONTEXT_MAX_SOURCES:-4}
  PLAYWRIGHT_SOCIAL_CONTEXT_QUERY_LENGTH: ${PLAYWRIGHT_SOCIAL_CONTEXT_QUERY_LENGTH:-120}
  PLAYWRIGHT_SOCIAL_CONTEXT_MIN_TERMS: ${PLAYWRIGHT_SOCIAL_CONTEXT_MIN_TERMS:-2}
  PLAYWRIGHT_VERIFICATION_COLLECTION_ENABLED: ${PLAYWRIGHT_VERIFICATION_COLLECTION_ENABLED:-true}
  PLAYWRIGHT_VERIFICATION_POST_LIMIT: ${PLAYWRIGHT_VERIFICATION_POST_LIMIT:-3}
  PLAYWRIGHT_VERIFICATION_SCROLL_LIMIT: ${PLAYWRIGHT_VERIFICATION_SCROLL_LIMIT:-2}
  PLAYWRIGHT_VERIFICATION_MAX_ITEMS: ${PLAYWRIGHT_VERIFICATION_MAX_ITEMS:-8}
  PLAYWRIGHT_VERIFICATION_QUERY_LENGTH: ${PLAYWRIGHT_VERIFICATION_QUERY_LENGTH:-140}
  PLAYWRIGHT_ANALYZE_POSTS: ${PLAYWRIGHT_ANALYZE_POSTS:-false}
  PLAYWRIGHT_AUTOMATION_TOKEN: ${PLAYWRIGHT_AUTOMATION_TOKEN:-}
  PLAYWRIGHT_LOG_CHANNEL: ${PLAYWRIGHT_LOG_CHANNEL:-playwright}
  DEEPFAKE_MODEL_ENABLED: ${DEEPFAKE_MODEL_ENABLED:-false}
  TRUSTED_PROXIES: ${TRUSTED_PROXIES:-*}
  VITE_APP_NAME: ${VITE_APP_NAME:-TruthGuard}
  VIEW_COMPILED_PATH: ${VIEW_COMPILED_PATH:-/var/www/html/bootstrap/cache/views}

x-app: &app
  image: truthguard:${IMAGE_TAG:-local}
  build: .
  environment:
    <<: *app-environment
  restart: unless-stopped
  init: true
  volumes:
    - storage:/var/www/html/storage
  depends_on:
    db:
      condition: service_healthy
  logging:
    driver: json-file
    options:
      max-size: "10m"
      max-file: "3"

services:
  app:
    <<: *app
    networks:
      - truthguard-internal
      - dokploy-network
    ports:
      - "${HTTP_BIND:-127.0.0.1}:${HTTP_PORT:-8080}:80"
    healthcheck:
      test: ["CMD", "php", "-r", "exit(@file_get_contents('http://127.0.0.1/up') === false ? 1 : 0);"]
      interval: 30s
      timeout: 10s
      start_period: 60s
      retries: 3

  worker:
    <<: *app
    networks:
      - truthguard-internal
    user: www-data
    command: ["php", "artisan", "queue:work", "--sleep=3", "--tries=3", "--timeout=300", "--max-time=3600"]
    stop_grace_period: 330s

  scheduler:
    <<: *app
    networks:
      - truthguard-internal
    user: www-data
    command: ["php", "artisan", "schedule:work"]
    stop_grace_period: 330s

  db:
    image: mysql:8.4
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_DATABASE:-truthguard}
      MYSQL_USER: ${DB_USERNAME:-truthguard}
      MYSQL_PASSWORD: ${DB_PASSWORD:?Set DB_PASSWORD in .env.production}
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:?Set DB_ROOT_PASSWORD in .env.production}
    networks:
      - truthguard-internal
    volumes:
      - database:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 --silent"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 90s

volumes:
  storage:
  database:

networks:
  truthguard-internal:
    driver: bridge
  dokploy-network:
    external: true
